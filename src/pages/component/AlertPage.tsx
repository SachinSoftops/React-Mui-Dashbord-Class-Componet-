import React, { Component } from 'react';

interface AlertPageProps {
  // Define your props here if needed
}

class AlertPage extends Component<AlertPageProps> {
  render() {
    return (
      <div>AlertPage</div>
    );
  }
}

export default AlertPage;
